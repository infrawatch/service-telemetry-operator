global:
  timeout: 2400
  env:
    OCP_PROJECT: __commit__
script:
  - echo === Creating project ; oc new-project service-telemetry
  - echo === Getting related projects and importing local builds ; ./build/ci_local_images.sh
after_script:
  - echo === Final State; echo
  - oc get pods
  - echo
  - ./deploy/containerlogs.sh
  - ./deploy/remove_stf.sh

